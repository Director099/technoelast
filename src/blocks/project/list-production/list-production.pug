mixin list-production({title})
  .section-content
    +title-wrap(title)
      a.icon-btn.icon-btn--blue() Все
    .section-content__wrap
      .grid-row
        -for(var i = 0; i < 8; i++)
          .grid-row__col.grid-row__col--xl-6.section-content__item-block
            +tovar()
      button(type="button").btn.btn--lighten.section-content__btn.d-none.d-xl-block Показать еще
